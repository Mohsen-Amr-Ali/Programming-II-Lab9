To compile:

cd ./src
javac -d ../out Main.java Factory/*.java Models/*.java Validators/*.java Validators/Workers/*.java

=======================
To create jar:

cd ../out
jar cfm SudokuValidator.jar ../src/manifest.txt *.class Factory/*.class Models/*.class Validators/*.class Validators/Workers/*.class
=======================
To run jar:

java -jar SudokuValidator.jar ..\test-csv-files\row_error.csv 3

but change the file path as necessary